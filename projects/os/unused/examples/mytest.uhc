{
/*	let c = 15;

	let i = 1 << 2*8;

	let is_identifier_char =
		c >= 'a' && c <= 'z' ||
		c >= 'A' && c <= 'Z' ||
		c >= '0' && c <= '9' ||
		c == '_';

	is_identifier_char = !(c > 5) || c == 15 || c == 78;

	is_identifier_char = is_identifier_char;*/

/*	let n = 0;
	let a = 0, b = 1;
	while(n < 10)
	{
		let t = a;
		a = b;
		b = t + b;
		++n;
	}

	print(a);*/

	for(let i = 0; i < 10; ++i)
	{

	}

	let i = 0;
	while(i < 10)
	{
		if()

		++i;
	}

	print(0xFF);
}
