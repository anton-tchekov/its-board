fn count(n)
{
	if((n & 1) == 0)
	{
		return 0;
	}

	return 1;
}

fn fib(n)
{
	if(n < 2) { return n; }
	return fib(n - 2) + fib(n - 1);
}

fn main()
{
	int i = 0, cnt = 0;
	while(i <= 100)
	{
		if(count(i))
		{
			++cnt;
		}

		++i;
	}
	test(cnt);
	test(fib(40));
}
